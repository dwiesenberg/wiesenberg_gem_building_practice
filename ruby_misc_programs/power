# Exponential calculations without using exxponent

def

  base = 2; exp = 4; result = 1   # exp = exponent

  (1..exp).each do
    result = result * exp
  end

  puts "#{base} to the power of #{exp} = #{result}"

end
